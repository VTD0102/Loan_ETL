name: loan_dbt
version: "1.0"
config-version: 2

profile: loan_dbt

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:

  loan_dbt:

    bronze:
      +schema: bronze
      +materialized: table

    silver:
      +schema: silver
      +materialized: table

    gold:
      +schema: gold
      +materialized: table