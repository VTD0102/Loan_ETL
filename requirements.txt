# Aggregate install for local full-stack Python work.
# Prefer scoped installs when working on a single layer:
#   pip install -r backend/requirements.txt
#   pip install -r machinelearning/requirements.txt

-r backend/requirements.txt
-r machinelearning/requirements.txt
