project:
  name: loan_data_platform
  environment: dev

database:
  host: localhost
  port: 5433
  name: loan_dw
  user: postgres
  password: "postgres"
  pool_size: 5
  max_overflow: 10
  pool_recycle: 1800

paths:
  raw_data: data/raw/prosperLoanData.csv

schemas:
  bronze: bronze
  silver: silver
  gold: gold

tables:
  raw_loans: prosper_loans_raw

ingestion:
  chunk_size: 10000
